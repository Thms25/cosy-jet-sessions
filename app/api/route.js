import { sql } from "@vercel/postgres";

export default async function VideoIds({ params }) {
  // const { rows } = await sql`SELECT * from VIDEO_ID`;

  // return (
  //   <div>
  //     {rows.map((row) => (
  //       <div key={row.id}>
  //         {row.id} - {row.video_yt_id}
  //       </div>
  //     ))}
  //   </div>
  // );
}
